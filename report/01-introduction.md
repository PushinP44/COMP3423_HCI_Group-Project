# 1. Introduction (~1 page)

*Owner: Member 1 (Project Lead & UX Strategist). Rubric: 8% total — problem statement + target user group (2), justification of app choice (2), scope states the 5 features (2), scope realistic for 5 students/8 weeks (2).*

## 1.1 The chosen application and its context

[FILL IN — name the specific real bank/app once confirmed in Week 1. State what it does and why mobile banking specifically is a meaningful HCI domain (draft angle from the plan: high-stakes tasks + low error tolerance + trust).]

Draft framing to build from (`2026-09-16-comp3423-project-plan.md`, Part 2):

> Mobile banking apps sit at the intersection of high task stakes (moving money, checking balances) and low error tolerance — a poorly designed flow doesn't just frustrate users, it erodes trust and can cause real financial mistakes.

## 1.2 Target user group

[FILL IN once User Researcher confirms — starting hypothesis from the plan: working adults who do most of their banking via the mobile app rather than branch visits, comfortable with smartphones but not power users.]

## 1.3 Problem statement

[FILL IN — one clear paragraph: why does the existing app fail this user group? Should synthesize, not list, the findings from `research/heuristic-evaluation.md` and `research/candidate-usability-problems.md` once those are populated with real findings.]

## 1.4 Scope: the 5 features

**Draft feature list — confirm/replace once the real heuristic evaluation (`research/heuristic-evaluation.md`) is done on the actual chosen app; these were chosen to match the draft justification in §1.1, not real findings yet. Prototyping happens directly in Figma (see `assets/figma/README.md`).**

1. **Home Dashboard & Quick Actions** — surfaces the most frequent actions (transfer, pay bills, deposit, cards) directly on the home screen instead of burying them in menus.
2. **Simplified Transfer Flow** — collapses a multi-step confirmation flow into 2 steps with a clear review screen, plus explicit success/failure feedback.
3. **Bill Pay Findability** — a dedicated, searchable bill-pay entry point with saved billers surfaced up front.
4. **Transaction Status Visibility** — every transaction shows an explicit, color-and-text status (completed/processing/failed), with a failed transaction stating clearly that no funds moved.
5. **Accessible Account Overview** — 44×44pt+ touch targets, visible focus states, labels that don't rely on color alone, and screen-reader-friendly balance labels.
